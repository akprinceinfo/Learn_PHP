CREATE TABLE tableName(
    if INT(8) NOT NULL AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    class VARCHAR(16) NOT NULL,
    roll INT(3), UNIQUE,
    admission_date DATE,
    PRIMARY KEY(id)


) ;